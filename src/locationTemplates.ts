import services from "../data/services.json";
import articles from "../data/articles.json";
import type { StateItem } from "./sitemaps";
import { SITE } from "../lib/site";

const DOMAIN = SITE.domain;
const PHONE_DISPLAY = SITE.phoneDisplay;
const PHONE_HREF = SITE.phoneHref;

const IMAGES = {
  state: "https://images.pexels.com/photos/34859642/pexels-photo-34859642.jpeg?auto=compress&cs=tinysrgb&w=1600",
  city: "https://images.pexels.com/photos/28384143/pexels-photo-28384143.jpeg?auto=compress&cs=tinysrgb&w=1600",
  service: "https://images.pexels.com/photos/34711989/pexels-photo-34711989.jpeg?auto=compress&cs=tinysrgb&w=1600",
};

const CSS = `
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:#fff;color:#172033;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",Arial,sans-serif;-webkit-font-smoothing:antialiased}a{color:inherit;text-decoration:none}.wrap{width:min(1180px,calc(100% - 32px));margin:auto}.top{background:#063a2c;color:#dbe7f5;font-size:12px}.top .wrap,.nav .wrap{display:flex;align-items:center;justify-content:space-between;gap:20px}.top .wrap{padding:9px 0}.nav{position:sticky;top:0;z-index:30;background:rgba(255,255,255,.97);backdrop-filter:blur(14px);border-bottom:1px solid #dfe6ee;box-shadow:0 10px 32px rgba(16,24,38,.08)}.nav .wrap{padding:14px 0}.brand{display:flex;align-items:center;gap:11px;font-size:20px;font-weight:950;color:#101826;letter-spacing:-.02em}.logo{width:44px;height:44px;border-radius:12px;display:grid;place-items:center;background:linear-gradient(135deg,#059669,#047857);color:#fff;box-shadow:0 10px 24px rgba(5,150,105,.25)}.brand small{display:block;font-size:9px;letter-spacing:.14em;text-transform:uppercase;color:#6c7a8b}.links{display:flex;gap:22px;font-size:14px;font-weight:850}.links a:hover{color:#059669}.btn{display:inline-flex;align-items:center;justify-content:center;min-height:48px;padding:14px 21px;border-radius:10px;background:#d97706;color:#fff;font-weight:900;box-shadow:0 10px 24px rgba(217,119,6,.24);transition:.2s}.btn:hover{transform:translateY(-2px);background:#b45309}.btn.dark{background:#063a2c}.btn.ghost{background:transparent;border:1px solid rgba(255,255,255,.38);box-shadow:none}.hero{position:relative;overflow:hidden;background:linear-gradient(135deg,#042f2e 0%,#064e3b 58%,#059669 100%);color:#fff;padding:78px 0}.hero-grid{display:grid;grid-template-columns:1.05fr .95fr;gap:48px;align-items:center}.hero h1{font-size:clamp(44px,6vw,72px);line-height:1.01;letter-spacing:-.052em;margin:20px 0}.hero h1 em{font-style:normal;color:#6ee7b7}.hero p{font-size:18px;line-height:1.75;color:#d6e2ee;max-width:760px}.hero-photo{position:relative}.hero-photo img{display:block;width:100%;height:470px;object-fit:cover;border-radius:25px;box-shadow:0 32px 75px rgba(0,0,0,.34);border:1px solid rgba(255,255,255,.18)}.hero-photo:after{content:"";position:absolute;right:-14px;top:-14px;width:100px;height:100px;border-top:8px solid #d97706;border-right:8px solid #d97706;border-radius:0 22px 0 0}.crumb{font-size:13px;color:#b7cad9}.crumb a{color:#a7f3d0}.eyebrow{display:inline-flex;margin-top:18px;padding:8px 12px;border-radius:999px;background:rgba(5,150,105,.2);border:1px solid rgba(110,231,183,.4);color:#a7f3d0;font-size:11px;font-weight:900;letter-spacing:.14em;text-transform:uppercase}.buttons{display:flex;flex-wrap:wrap;gap:12px;margin-top:27px}.badges{display:flex;flex-wrap:wrap;gap:10px;margin-top:22px}.badge{padding:9px 12px;border-radius:999px;background:rgba(255,255,255,.09);border:1px solid rgba(255,255,255,.18);font-size:12px;font-weight:850;color:#eaf4fb}.stats{border-bottom:1px solid #dfe6ee;background:#fff}.stats .wrap{display:grid;grid-template-columns:repeat(4,1fr)}.stat{text-align:center;padding:27px 15px;border-left:1px solid #dfe6ee}.stat:first-child{border-left:0}.stat strong{display:block;font-size:31px;color:#064e3b}.stat span{display:block;margin-top:5px;color:#778495;font-size:11px;font-weight:900;text-transform:uppercase;letter-spacing:.1em}.section{padding:78px 0}.soft{background:#f0fdf4}.blue{background:#ecfdf5}.dark-section{background:#042f2e;color:#fff}.head{display:flex;align-items:end;justify-content:space-between;gap:28px;margin-bottom:32px}.eyeline{display:inline-block;color:#059669;font-size:11px;font-weight:900;letter-spacing:.13em;text-transform:uppercase}.section h2{font-size:clamp(34px,4vw,50px);line-height:1.08;margin:8px 0 0;letter-spacing:-.038em}.muted{max-width:760px;color:#667486;line-height:1.75}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:20px}.card{display:block;background:#fff;border:1px solid #dfe6ee;border-radius:18px;padding:25px;box-shadow:0 8px 26px rgba(16,24,38,.06);transition:.2s}.card:hover{transform:translateY(-4px);border-color:#34d399;box-shadow:0 18px 40px rgba(16,24,38,.12)}.card b{display:grid;place-items:center;width:46px;height:46px;border-radius:13px;background:#d1fae5;color:#059669;font-size:14px}.card h3{font-size:20px;margin:17px 0 9px;color:#101826;letter-spacing:-.02em}.card p{color:#667486;line-height:1.68;margin:0;font-size:14px}.more{display:inline-block;margin-top:17px;color:#059669;font-weight:900;font-size:14px}.directory{display:grid;grid-template-columns:repeat(4,1fr);gap:13px}.directory a{display:flex;align-items:center;justify-content:space-between;gap:15px;padding:17px 18px;border:1px solid #dfe6ee;border-radius:13px;background:#fff;color:#344054;font-size:14px;font-weight:850;box-shadow:0 6px 18px rgba(16,24,38,.04);transition:.18s}.directory a:after{content:"→";color:#059669}.directory a:hover{transform:translateY(-2px);color:#059669;border-color:#34d399}.content{display:grid;grid-template-columns:minmax(0,1fr) 340px;gap:40px}.article{font-size:17px;line-height:1.82}.article h2{font-size:30px;color:#101826;margin-top:40px;letter-spacing:-.025em}.article h3{font-size:21px;color:#101826}.article p,.article li{color:#5e6d7e}.article li{margin:8px 0}.side{position:sticky;top:105px;align-self:start;background:linear-gradient(145deg,#042f2e,#064e3b);color:#fff;border-radius:19px;padding:27px;box-shadow:0 18px 45px rgba(16,24,38,.2)}.side p{color:#d5e2ee;line-height:1.65}.side .more{display:block;color:#a7f3d0}.notice{background:#fff7ed;border:1px solid #fed7aa;border-radius:14px;padding:19px;color:#9a3412;line-height:1.65}.process{display:grid;grid-template-columns:repeat(4,1fr);gap:18px;margin-top:34px}.step{background:#fff;border:1px solid #dfe6ee;border-radius:16px;padding:23px}.step b{font-size:35px;color:#dce7ef}.step h3{font-size:20px;color:#101826}.step p{color:#667486;line-height:1.65}.faq{display:grid;gap:12px;margin-top:28px}.faq details{background:#fff;border:1px solid #dfe6ee;border-radius:13px;padding:18px 20px}.faq summary{cursor:pointer;font-weight:900;color:#101826}.faq p{color:#667486;line-height:1.7}.footer{background:#042f2e;color:#aebdca;padding:54px 0 22px}.footer .wrap{display:grid;grid-template-columns:1.2fr 1fr 1fr;gap:36px}.footer h3{color:#fff}.footer a{display:block;margin:10px 0}.legal{grid-column:1/-1;border-top:1px solid rgba(255,255,255,.1);padding-top:20px;font-size:12px}.sticky{position:fixed;right:18px;bottom:18px;z-index:80}@media(max-width:920px){.links{display:none}.hero-grid,.content{grid-template-columns:1fr}.hero-photo img{height:420px}.grid{grid-template-columns:repeat(2,1fr)}.directory{grid-template-columns:repeat(2,1fr)}.process{grid-template-columns:repeat(2,1fr)}.side{position:static}.footer .wrap{grid-template-columns:1fr 1fr}}@media(max-width:620px){.top span:last-child{display:none}.hero{padding:58px 0}.hero h1{font-size:44px}.hero-photo img{height:360px}.grid,.directory,.process,.footer .wrap{grid-template-columns:1fr}.stats .wrap{grid-template-columns:1fr 1fr}.stat:nth-child(3){border-left:0;border-top:1px solid #dfe6ee}.stat:nth-child(4){border-top:1px solid #dfe6ee}.head{display:block}.btn{width:100%}.sticky{left:12px;right:12px;bottom:12px}}
`;

function esc(value: string) {
  return value.replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[char] || char);
}

function header() {
  return `<div class="top"><div class="wrap"><span>Nationwide certified arborist information and tree service provider directory</span><span>Independent providers · Call ${PHONE_DISPLAY}</span></div></div><header class="nav"><div class="wrap"><a class="brand" href="https://${DOMAIN}/"><span class="logo">CT</span><span>${SITE.name}<small>Tree Removal · Trimming · Stump Grinding</small></span></a><nav class="links"><a href="https://${DOMAIN}/services/">Services</a><a href="https://${DOMAIN}/locations/">Service Areas</a><a href="https://${DOMAIN}/articles/">Arborist Guides</a><a href="https://${DOMAIN}/about/">About</a><a href="https://${DOMAIN}/contact/">Contact</a></nav><a class="btn" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a></div></header>`;
}

function footer() {
  return `<footer class="footer"><div class="wrap"><div><h3>${SITE.name}</h3><p>Research tree care topics, emergency removal guidelines, and arborist services across all 50 states.</p><a class="btn" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a></div><div><h3>Explore</h3><a href="https://${DOMAIN}/services/">All ${services.length} Services</a><a href="https://${DOMAIN}/locations/">States &amp; Cities</a><a href="https://${DOMAIN}/articles/">Arborist Guides</a><a href="https://${DOMAIN}/about/">About</a></div><div><h3>Disclosure</h3><a href="https://${DOMAIN}/provider-disclosure/">Provider Disclosure</a><a href="https://${DOMAIN}/privacy-policy/">Privacy Policy</a><a href="https://${DOMAIN}/terms/">Terms</a><a href="https://${DOMAIN}/disclaimer/">Disclaimer</a><a href="https://${DOMAIN}/accessibility/">Accessibility</a></div><div class="legal">© ${new Date().getUTCFullYear()} ${SITE.name}. This website is an information and referral platform. Providers are independent businesses. Verify current coverage, legal identity, credentials, insurance, scope, pricing and warranties before hiring.</div></div></footer>`;
}

function shell(title: string, description: string, canonical: string, body: string, schema: unknown) {
  return `<!doctype html><html lang="en-US"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} | ${SITE.name}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${canonical}"><meta name="robots" content="index,follow"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${canonical}"><style>${CSS}</style><script type="application/ld+json">${JSON.stringify(schema).replace(/</g, "\\u003c")}</script></head><body>${header()}${body}${footer()}<a class="btn sticky" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a></body></html>`;
}

function serviceCards(host: string, local: boolean) {
  return services.map((service, index) => `<a class="card" href="${local ? `https://${host}/${service.slug}/` : `https://${DOMAIN}/services/${service.slug}/`}"><b>${String(index + 1).padStart(2, "0")}</b><h3>${esc(service.name)}</h3><p>${esc(service.description)}</p><span class="more">Review service →</span></a>`).join("");
}

export function staticPage(title: string, path: string, contentHtml: string) {
  const canonical = `https://${DOMAIN}${path}`;
  const schema = {
    "@context": "https://schema.org",
    "@type": "WebPage",
    "name": title,
    "url": canonical,
    "publisher": { "@type": "Organization", "name": SITE.name, "url": `https://${DOMAIN}/` }
  };
  const body = `<main><section class="hero" style="padding:48px 0"><div class="wrap"><h1>${esc(title)}</h1><div class="crumb"><a href="https://${DOMAIN}/">Home</a> / ${esc(title)}</div></div></section><section class="section"><div class="wrap content"><article class="article">${contentHtml}</article><aside class="side"><h2>Need Assistance?</h2><p>Call our 24/7 arborist referral line.</p><a class="btn" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a></aside></div></section></main>`;
  return shell(title, `${title} - ${SITE.name}`, canonical, body, schema);
}

export function statePage(state: StateItem, host: string) {
  const cityLinks = state.cities.map(([slug, name]) => `<a href="https://${slug}-${state.slug}.${DOMAIN}/"><span>${esc(name)}</span></a>`).join("");
  const canonical = `https://${host}/`;
  const schema = { "@context": "https://schema.org", "@graph": [{ "@type": "CollectionPage", name: `Tree Services in ${state.name}`, url: canonical, about: { "@type": "State", name: state.name }, isPartOf: { "@type": "WebSite", name: SITE.name, url: `https://${DOMAIN}/` } }, { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: "Service Areas", item: `https://${DOMAIN}/locations/` }, { "@type": "ListItem", position: 2, name: state.name, item: canonical }] }] };
  const body = `<main><section class="hero"><div class="wrap hero-grid"><div><div class="crumb"><a href="https://${DOMAIN}/">Home</a> / <a href="https://${DOMAIN}/locations/">Service Areas</a> / ${esc(state.name)}</div><span class="eyebrow">${state.code.toUpperCase()} tree service directory</span><h1>Tree services across <em>${esc(state.name)}</em></h1><p>Choose a city or community, review the complete ${services.length}-service directory and prepare the information needed to confirm independent arborist coverage, inspection, scope, and price.</p><div class="buttons"><a class="btn" href="#cities">Browse ${state.cities.length.toLocaleString()} Cities</a><a class="btn ghost" href="#services">View All Services</a></div><div class="badges"><span class="badge">Certified Arborists</span><span class="badge">City-specific routes</span><span class="badge">No fabricated offices</span></div></div><div class="hero-photo"><img src="${IMAGES.state}" alt="Tree care and arborist service area" width="800" height="620"></div></div></section><section class="stats"><div class="wrap"><div class="stat"><strong>${services.length}</strong><span>Service topics</span></div><div class="stat"><strong>${state.cities.length.toLocaleString()}</strong><span>Cities &amp; communities</span></div><div class="stat"><strong>${state.code.toUpperCase()}</strong><span>State directory</span></div><div class="stat"><strong>Direct</strong><span>Provider verification</span></div></div></section><section class="section soft" id="cities"><div class="wrap"><div class="head"><div><span class="eyeline">Service Areas</span><h2>Tree service locations in ${esc(state.name)}</h2><p class="muted">Select a city to open its local service hub. Geographic pages organize information for easy access.</p></div></div><div class="directory">${cityLinks}</div></div></section><section class="section" id="services"><div class="wrap"><div class="head"><div><span class="eyeline">Complete directory</span><h2>All ${services.length} tree care services</h2><p class="muted">Review removal, trimming, stump grinding, risk assessment, cabling, storm clearance, and commercial tree topics.</p></div><a class="btn dark" href="https://${DOMAIN}/services/">National Service Hub</a></div><div class="grid">${serviceCards(host, false)}</div></div></section></main>`;
  return shell(`Tree Services in ${state.name}`, `Browse ${services.length} tree care services and ${state.cities.length} city routes in ${state.name}.`, canonical, body, schema);
}

export function cityPage(state: StateItem, city: [string, string], host: string) {
  const [, cityName] = city;
  const canonical = `https://${host}/`;
  const schema = { "@context": "https://schema.org", "@graph": [{ "@type": "CollectionPage", name: `Tree Services in ${cityName}, ${state.name}`, url: canonical, about: { "@type": "City", name: cityName, containedInPlace: { "@type": "State", name: state.name } }, isPartOf: { "@type": "WebSite", name: SITE.name, url: `https://${DOMAIN}/` } }, { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: state.name, item: `https://${state.slug}.${DOMAIN}/` }, { "@type": "ListItem", position: 2, name: cityName, item: canonical }] }] };
  const body = `<main><section class="hero"><div class="wrap hero-grid"><div><div class="crumb"><a href="https://${DOMAIN}/locations/">Service Areas</a> / <a href="https://${state.slug}.${DOMAIN}/">${esc(state.name)}</a> / ${esc(cityName)}</div><span class="eyebrow">Local tree service guide</span><h1>Tree service in <em>${esc(cityName)}, ${esc(state.name)}</em></h1><p>Explore the complete ${services.length}-service directory for ${esc(cityName)}. Identify component issues, structural hazards, and prepare for a direct arborist consultation.</p><div class="buttons"><a class="btn" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a><a class="btn ghost" href="#services">Browse All Services</a></div><div class="badges"><span class="badge">Location-specific guide</span><span class="badge">Safety-conscious</span><span class="badge">Written-scope checklist</span></div></div><div class="hero-photo"><img src="${IMAGES.city}" alt="Tree care and arborist service in local city" width="800" height="620"></div></div></section><section class="stats"><div class="wrap"><div class="stat"><strong>${services.length}</strong><span>Local service topics</span></div><div class="stat"><strong>${state.code.toUpperCase()}</strong><span>${esc(state.name)}</span></div><div class="stat"><strong>City</strong><span>${esc(cityName)}</span></div><div class="stat"><strong>Verify</strong><span>Scope &amp; price</span></div></div></section><section class="section soft" id="services"><div class="wrap"><div class="head"><div><span class="eyeline">Tree care services</span><h2>Services to review in ${esc(cityName)}</h2><p class="muted">Select the exact tree care symptom or project for detailed inspection guidance and provider questions.</p></div></div><div class="grid">${serviceCards(host, true)}</div></div></section></main>`;
  return shell(`Tree Service in ${cityName}, ${state.name}`, `Browse ${services.length} tree removal, trimming, and arborist service topics for ${cityName}, ${state.name}.`, canonical, body, schema);
}

export function localServicePage(state: StateItem, city: [string, string], service: (typeof services)[number], host: string) {
  const [, cityName] = city;
  const canonical = `https://${host}/${service.slug}/`;
  const related = services.filter((item) => item.category === service.category && item.slug !== service.slug).slice(0, 6);
  const schema = { "@context": "https://schema.org", "@graph": [{ "@type": "Service", name: `${service.name} in ${cityName}, ${state.name}`, serviceType: service.category, description: service.description, url: canonical, areaServed: { "@type": "City", name: cityName, containedInPlace: { "@type": "State", name: state.name } }, provider: { "@type": "Organization", name: SITE.name, url: `https://${DOMAIN}/` } }, { "@type": "BreadcrumbList", itemListElement: [{ "@type": "ListItem", position: 1, name: state.name, item: `https://${state.slug}.${DOMAIN}/` }, { "@type": "ListItem", position: 2, name: cityName, item: `https://${host}/` }, { "@type": "ListItem", position: 3, name: service.name, item: canonical }] }] };
  const body = `<main><section class="hero"><div class="wrap hero-grid"><div><div class="crumb"><a href="https://${state.slug}.${DOMAIN}/">${esc(state.name)}</a> / <a href="https://${host}/">${esc(cityName)}</a> / ${esc(service.name)}</div><span class="eyebrow">${esc(service.category)}</span><h1>${esc(service.name)} in <em>${esc(cityName)}, ${esc(state.name)}</em></h1><p>${esc(service.description)} Review warning signs, safety rigging procedures, and questions to confirm before authorizing work.</p><div class="buttons"><a class="btn" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a><a class="btn ghost" href="https://${host}/">All ${services.length} City Services</a></div></div><div class="hero-photo"><img src="${IMAGES.service}" alt="Tree removal and arborist service" width="800" height="620"></div></div></section><section class="section soft"><div class="wrap"><div class="head"><div><span class="eyeline">What to expect</span><h2>A diagnosis-first service process</h2></div></div><div class="process"><div class="step"><b>01</b><h3>Describe the issue</h3><p>Share tree size, leaning status, proximity to roofs/power lines, and storm urgency.</p></div><div class="step"><b>02</b><h3>Arborist evaluation</h3><p>Assess root stability, wood decay, canopy weight, and structural hazards.</p></div><div class="step"><b>03</b><h3>Compare options</h3><p>Review pruning, cabling, stump grinding, or complete removal options.</p></div><div class="step"><b>04</b><h3>Approve in writing</h3><p>Confirm equipment rigging, insurance, total price, and cleanup before work starts.</p></div></div></div></section><section class="section"><div class="wrap content"><article class="article"><span class="eyeline">Service overview</span><h2>Start with an accurate arborist diagnosis</h2><p>Proper tree care requires experienced rigging, high-reach equipment, and certified arborist safety protocols. Review property risk factors and confirm credentials before starting heavy removal or trimming work.</p><h2>Common warning signs</h2><ul><li>Tree trunk leans severely or shows ground soil heaving around the root plate.</li><li>Large dead branches or cracked limbs hanging in the canopy ('widowmakers').</li><li>Fungal mushroom growth, hollow trunk cavities, or peeling bark at the tree base.</li><li>Overhanging heavy branches touching rooflines or power cables.</li></ul><h2>Questions to ask before approving work</h2><ul><li>Is the tree removal crew fully insured for property damage and workers' comp?</li><li>Does the quote include heavy crane rigging, brush chipping, and stump grinding?</li><li>Are city permits required for this tree species or size in ${esc(cityName)}?</li></ul></article><aside class="side"><span class="eyebrow">${esc(cityName)} coverage</span><h2>${esc(service.name)}</h2><p>${esc(cityName)}, ${esc(state.name)}</p><p>Confirm arborist availability, equipment rigging, total price and warranty terms directly.</p><a class="btn" href="${PHONE_HREF}">Call ${PHONE_DISPLAY}</a><h3>Related services</h3>${related.map((item) => `<a class="more" href="https://${host}/${item.slug}/">${esc(item.name)} →</a>`).join("")}</aside></div></section></main>`;
  return shell(`${service.name} in ${cityName}, ${state.name}`, `${service.description} Service information for ${cityName}, ${state.name}.`, canonical, body, schema);
}

export function notFoundPage(message: string) {
  return `<!doctype html><html><head><meta name="robots" content="noindex"><meta name="viewport" content="width=device-width,initial-scale=1"><title>404 | ${SITE.name}</title><style>${CSS}</style></head><body>${header()}<main class="section"><div class="wrap"><span class="eyeline">Page not found</span><h1>404</h1><p>${esc(message)}</p><a class="btn dark" href="https://${DOMAIN}/locations/">Browse Service Areas</a></div></main>${footer()}</body></html>`;
}
